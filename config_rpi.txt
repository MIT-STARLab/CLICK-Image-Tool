# RPi boot configuration
kernel=kernel.img       # Kernel image filename
gpu_mem=16              # Allocate minimum RAM for GPU
core_freq_min=350       # Fix GPU clock to make SCLK correct and stable
core_freq=350           # https://github.com/raspberryPI/linux/issues/2094
dtparam=audio=off       # Disable audio init
hdmi_blanking=2         # Disable HDMI PHY output
disable_splash=1        # Disable splash screen on boot
force_eeprom_read=0     # Disable search for any HATs
dtoverlay=click_spi     # Enable CLICK SPI driver
dtparam=uart0=on        # Enable UART0
